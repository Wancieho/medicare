import Header from "./layout/Header/header";
import { HerbsDropdown, PlantsDropdown, SpicesDropdown, ExtractsDropdown }
    from "./layout/Header/headerData";
let AppImports = {
    Header, HerbsDropdown, PlantsDropdown, SpicesDropdown, ExtractsDropdown
}
export default AppImports;
